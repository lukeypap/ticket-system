import React from "react";
import { LoginCard } from "../src/components/login";

interface Props {}

const Login = (props: Props) => {
    return (
        <div>
            <LoginCard />
        </div>
    );
};

export default Login;
